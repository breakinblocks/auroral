name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 25

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build
        run: chmod +x gradlew && ./gradlew clean build

      - name: Resolve version
        id: version
        run: echo "version=$(sed -n 's/^mod_version=//p' gradle.properties | tr -d '\r')" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: build/libs/auroral-*.jar
          body_path: metadata/changelogs/${{ steps.version.outputs.version }}.md
          fail_on_unmatched_files: true
